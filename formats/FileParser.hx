package formats;


class FileParser 
{
    public var libs:Array<String> = [];
    public var libsVersion:Array<String> = [];
    
    public function new(path:String)
    {
        parse(path);
    }

    public function parse(path:String)
    {
        
    }
}